# Test and development dependencies for the kitchen application
# Install with: pip install -r requirements-test.txt
# Note: This includes all production dependencies plus testing tools

# Include production dependencies
-r requirements.txt

# Core testing framework
pytest>=8.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
pytest-cov>=4.0.0  # Coverage reporting
diff-cover>=9.0.0  # Diff-based coverage gate (enforces coverage on changed lines only)

# HTTP testing for FastAPI
httpx>=0.27.0  # HTTP client for testing

# Additional testing utilities
freezegun>=1.2.0  # Time mocking
responses>=0.23.0  # HTTP mocking
factory-boy>=3.2.0  # Test data factories

# Development tools
ruff>=0.8.0  # Linting + formatting (replaces black, flake8, isort)
pip-audit>=2.7.0  # Dependency vulnerability scanning
mypy>=1.0.0  # Type checking
mypy-baseline>=0.7.0  # Gradual mypy adoption (baseline of existing errors)
radon>=5.1.0  # Cyclomatic complexity and maintainability index
wily>=1.25.0  # Complexity trend tracking over git history
mutmut>=3.5.0  # Mutation testing for critical business logic
vulture>=2.14  # Dead code detection (baseline in .vulture-baseline.txt)
import-linter>=2.11  # Layer boundary enforcement (contracts in .importlinter)
bandit>=1.9.0  # Security linting (baseline in .bandit-baseline.json)
sqlfluff>=3.0.0  # SQL linting for migrations (config in .sqlfluff, baseline in .sqlfluff-baseline.txt)

# Type stubs for mypy
types-requests>=2.31.0
types-PyYAML>=6.0.0
types-pytz>=2024.1
types-python-dateutil>=2.9.0
